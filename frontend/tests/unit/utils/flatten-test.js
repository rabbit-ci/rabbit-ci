import flatten from 'rabbit-ci/utils/flatten';
import { module, test } from 'qunit';

module('Unit | Utility | flatten');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = flatten();
  assert.ok(result);
});
